# Custom SSL Files

Place your custom keystore.jks and truststore.jks files in this directory.

These files are used for NiFi HTTPS when Keycloak authentication is enabled.

Note: These files are not committed to git (see .gitignore)
